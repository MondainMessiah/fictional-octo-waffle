import os
import requests

def send_to_discord(message):
    webhook_url = os.environ["DISCORD_WEBHOOK_URL"]
    payload = {"content": message}
    requests.post(webhook_url, json=payload)

# Example bot message
if __name__ == "__main__":
    send_to_discord("Tibia Daily Bot ran successfully!")
